# Results Directory

This directory is reserved for inference outputs generated by RXNRECer.

Public releases intentionally ship without unpublished benchmarks, evaluation artifacts,
queue outputs, or cached research results.

Typical runtime-generated subdirectories:

- `cache/` for reusable inference caches.
- `sample/` for example outputs.
- user-defined output paths for TSV or JSON predictions.
