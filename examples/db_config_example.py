"""Example placeholder for optional local database configuration."""

# Public releases do not require a database configuration for standard inference.
# If you maintain local mirrors or custom services, create your own config module
# based on project needs instead of editing package source files directly.

DB_HOST = "localhost"
DB_PORT = 5432
DB_NAME = "rxnrecer"
DB_USER = "user"
DB_PASSWORD = "password"

